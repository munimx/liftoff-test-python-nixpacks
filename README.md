# liftoff-test-python-nixpacks

Simple Python + Flask app with **no Dockerfile** to test Nixpacks detection.

## Local run

```bash
python3 -m venv .venv
source .venv/bin/activate
pip install -r requirements.txt
python app.py
```
